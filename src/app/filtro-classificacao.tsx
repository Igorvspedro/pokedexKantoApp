// Arquivo ghost: nunca é renderizado, pois o AppTabs intercepta o tabPress
// com e.preventDefault() para abrir o Modal de Filtro de Especialidade (Classificacao).
export default function FiltroClassificacaoScreen() {
  return null;
}
