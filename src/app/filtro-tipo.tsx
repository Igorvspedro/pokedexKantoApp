// Arquivo ghost: nunca é renderizado, pois o AppTabs intercepta o tabPress
// com e.preventDefault() para abrir o Modal de Filtro de Especialidade (Tipo).
export default function FiltroTipoScreen() {
  return null;
}
